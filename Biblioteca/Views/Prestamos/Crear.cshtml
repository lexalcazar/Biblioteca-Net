@model Biblioteca.Models.ViewModels.PrestamoCreateVm

@{
    ViewData["Title"] = "Crear préstamo";
}

<style>
    .form-container {
        max-width: 600px;
        margin: 40px auto;
        padding: 20px;
    }

    .form-card {
        background: white;
        border-radius: 15px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        padding: 30px;
    }

    .form-header {
        text-align: center;
        color: #007bff;
        font-size: 1.8rem;
        margin-bottom: 10px;
        border-bottom: 3px solid #007bff;
        padding-bottom: 15px;
    }

    .success-message {
        background: #d4edda;
        border: 1px solid #c3e6cb;
        border-radius: 8px;
        padding: 12px;
        margin: 15px 0;
        color: #155724;
    }

    .error-messages {
        background: #f8d7da;
        border: 1px solid #f5c6cb;
        border-radius: 8px;
        padding: 12px;
        margin: 15px 0;
        color: #721c24;
    }

    .form-group {
        margin-bottom: 20px;
    }

    .form-group label {
        font-weight: 600;
        color: #495057;
        margin-bottom: 8px;
        display: block;
    }

    .form-control {
        border-radius: 8px;
        border: 1px solid #ced4da;
        padding: 10px;
        width: 100%;
        transition: border-color 0.3s ease;
    }

    .form-control:focus {
        border-color: #007bff;
        outline: none;
        box-shadow: 0 0 0 0.2rem rgba(0, 123, 255, 0.25);
    }

    .text-danger {
        color: #dc3545;
        font-size: 0.875rem;
        margin-top: 5px;
        display: block;
    }

    .btn-custom {
        padding: 12px 30px;
        border-radius: 8px;
        font-weight: 500;
        transition: all 0.3s ease;
        border: none;
        cursor: pointer;
        width: 100%;
        margin-top: 10px;
    }

    .btn-primary {
        background: #007bff;
        color: white;
    }

    .btn-primary:hover {
        background: #0056b3;
        transform: translateY(-2px);
        box-shadow: 0 4px 8px rgba(0, 123, 255, 0.3);
    }
</style>

<div class="form-container">
    <div class="form-card">
        <h1 class="form-header">📤 Crear Préstamo</h1>

        @if (TempData["ok"] != null)
        {
            <div class="success-message">✅ @TempData["ok"]</div>
        }

        @if (ViewData.ModelState.Values.SelectMany(v => v.Errors).Any())
        {
            <div class="error-messages">
                @foreach (var err in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                {
                    <div>❌ @err.ErrorMessage</div>
                }
            </div>
        }

        <form asp-action="Crear" method="post">
            @Html.AntiForgeryToken()

            <div class="form-group">
                <label>📚 Libro</label>
                <select asp-for="LibroId" asp-items="Model.Libros" class="form-control">
                    <option value="">-- Seleccione un libro --</option>
                </select>
            </div>

            <div class="form-group">
                <label>📧 Email del usuario</label>
                <input asp-for="EmailUsuario" class="form-control" placeholder="ejemplo@correo.com" />
                <span asp-validation-for="EmailUsuario" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label>📅 Fecha inicio</label>
                <input asp-for="FechaInicio" type="date" class="form-control" />
            </div>

            <div class="form-group">
                <label>📆 Fecha fin</label>
                <input asp-for="FechaFin" type="date" class="form-control" />
            </div>

            <button type="submit" class="btn-custom btn-primary">✓ Crear préstamo</button>
        </form>
    </div>
</div>
